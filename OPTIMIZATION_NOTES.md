# 关键信息提取优化说明

## 🚀 性能提升

### 优化前
- **10 次独立 LLM 调用**（每个字段一次）
- 预计用时：**60-90 秒**
- Token 消耗：约 8,000-12,000 tokens

### 优化后
- **1 次批量 LLM 调用**（一次性提取所有字段）
- 预计用时：**10-15 秒**
- Token 消耗：约 1,500-2,500 tokens
- **速度提升：6-9倍**
- **成本降低：70-80%**

---

## 🔧 优化技术

### 1. 批量提取策略
```python
# ❌ 旧方法：循环调用
for field in fields:
    result = ask_question(query)  # 10次API调用
    
# ✅ 新方法：一次性提取
result = llm.predict(batch_prompt)  # 1次API调用
```

### 2. 精简上下文
- 只取前 8 个最相关的文档片段（而非所有）
- 上下文限制在 4000 字符内
- 减少不必要的 token 消耗

### 3. 直接 LLM 调用
- 跳过复杂的 ConversationalRetrievalChain
- 直接使用 `llm.predict()` 更快速

### 4. 智能缓存
- 首次提取后自动缓存到数据库
- 后续访问直接读取缓存（<1秒）
- 提供"重新提取"按钮清除缓存

---

## 📊 UI 改进

### 表格字段更新
- **旧**: "字段" → "值"
- **新**: "关键信息" → "具体内容"（更友好）

### 中文字段映射
```python
rent_amount      → 租金金额
lease_duration   → 租赁期限
security_deposit → 押金金额
...
```

### 增强功能
- ✅ 缓存提示（告知用户是否使用缓存）
- ✅ 进度指示器（显示预计耗时）
- ✅ 重新提取按钮（清除缓存）
- ✅ 错误处理（提取失败时友好提示）

---

## 💡 使用建议

1. **首次提取**：点击"提取关键信息"，等待 10-15 秒
2. **再次查看**：使用缓存，瞬间显示（<1秒）
3. **更新数据**：点击"重新提取"清除缓存后重新提取

---

## 🔍 技术细节

### 提示词工程
优化后的提示词采用：
- 结构化输出格式（键值对）
- 明确的回退策略（未找到 → "未提及"）
- 紧凑的问题列表（减少冗余）

### 解析逻辑
- 智能解析中英文冒号
- 健壮的错误处理
- 默认值填充机制

---

最后更新：2025-10-28
