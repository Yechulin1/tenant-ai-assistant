# requirements_advanced.txt
# 高级合同管理系统依赖

# Core
streamlit==1.28.0
pandas==2.0.3
numpy==1.24.3

# LangChain生态系统
langchain
langchain-community
langchain-openai
# OpenAI
openai==1.3.0

# PDF处理（安装所有，系统会自动选择最佳的）
pdfplumber==0.10.3    # 最好的表格提取
PyMuPDF==1.23.8       # 最准确的文本提取
pypdf2==3.0.1         # 基础备用

# 向量数据库
faiss-cpu==1.7.4      # CPU版本，如果有GPU可以用faiss-gpu

# 辅助工具
python-dotenv==1.0.0
tiktoken==0.5.1       # Token计数
chardet==5.2.0        # 字符编码检测

# 可选：增强功能
# sentence-transformers==2.2.2  # 如果要使用本地嵌入模型
# chromadb==0.4.0               # 替代向量数据库选项